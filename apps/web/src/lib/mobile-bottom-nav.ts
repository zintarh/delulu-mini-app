/** Matches spacer + fixed nav in `bottom-nav.tsx` */
export const MOBILE_BOTTOM_NAV_HEIGHT =
  "calc(56px + max(env(safe-area-inset-bottom), 8px))";

/** Padding for sticky footers / page bottoms so content clears the mobile tab bar */
export const MOBILE_BOTTOM_NAV_CLEARANCE =
  "calc(56px + max(env(safe-area-inset-bottom), 8px) + 12px)";
